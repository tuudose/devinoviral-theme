<section class="section" id="likeuri" style="background: var(--bg-secondary);">
  <div class="section-header fade-up">
    <h2>❤️ PACHETE <span class="gradient-text">LIKE-URI</span></h2>
    <p>Crește engagement-ul postărilor tale cu like-uri reale și rapide.</p>
  </div>
  <div class="packages-grid fade-up">
    {% assign product = all_products[section.settings.product_handle] %}

    <!-- 500 Like-uri -->
    <div class="package-card">
      <span class="package-badge badge-recommended">⭐ Recomandat</span>
      <div class="package-icon" style="background: linear-gradient(135deg, #F77737, #E1306C);">❤️</div>
      <div class="package-amount">500</div>
      <div class="package-type">Like-uri</div>
      <div class="package-price">19.99 <span class="package-currency">RON</span></div>
      <ul class="package-features">
        <li><span class="check">✓</span> Livrare Rapidă</li>
        <li><span class="check">✓</span> Pe orice postare</li>
        <li><span class="check">✓</span> Fără parolă</li>
        <li><span class="check">✓</span> Garanție completă</li>
      </ul>
      {% if product %}
        {% for variant in product.variants %}
          {% if variant.title contains '500 Like' %}
            <a href="/cart/{{ variant.id }}:1" class="package-btn">⚡ Cumpără Acum</a>
          {% endif %}
        {% endfor %}
      {% else %}
        <a href="/products/{{ section.settings.product_handle }}" class="package-btn">⚡ Cumpără Acum</a>
      {% endif %}
      <div class="package-delivery">⚡ Livrare Rapidă</div>
    </div>

    <!-- 1000 Like-uri -->
    <div class="package-card popular">
      <span class="package-badge badge-popular">🔥 Cel mai vândut</span>
      <div class="package-icon" style="background: linear-gradient(135deg, #F77737, #E1306C);">❤️</div>
      <div class="package-amount">1000</div>
      <div class="package-type">Like-uri</div>
      <div class="package-price">29.99 <span class="package-currency">RON</span></div>
      <ul class="package-features">
        <li><span class="check">✓</span> Livrare Rapidă</li>
        <li><span class="check">✓</span> Pe orice postare</li>
        <li><span class="check">✓</span> Fără parolă</li>
        <li><span class="check">✓</span> Garanție completă</li>
      </ul>
      <input type="url" class="package-link-input" placeholder="Link Instagram" style="width:100%;margin-bottom:10px;" required>
      {% if product %}
        {% for variant in product.variants %}
          {% if variant.title contains '1000 Like' %}
            <a href="/cart/{{ variant.id }}:1" class="package-btn" onclick="return sendLinkToMe(this)">⚡ Cumpără Acum</a>
          {% endif %}
        {% endfor %}
      {% else %}
        <a href="/products/{{ section.settings.product_handle }}" class="package-btn" onclick="return sendLinkToMe(this)">⚡ Cumpără Acum</a>
      {% endif %}
      <div class="package-delivery">⚡ Livrare Rapidă</div>
    </div>
<script>
function sendLinkToMe(btn) {
  var input = btn.parentElement.querySelector('.package-link-input');
  if (!input || !input.value) {
    input.style.border = '2px solid #e1306c';
    input.focus();
    return false;
  }
  // Poți trimite linkul către email sau webhook aici dacă ai backend
  // Exemplu: fetch('/save-link', {method:'POST',body:JSON.stringify({link:input.value})})
  // Sau doar trimite-l pe email manual din comenzi
  return true;
}
</script>

    <!-- 2500 Like-uri -->
    <div class="package-card">
      <span class="package-badge badge-save">💸 -33%</span>
      <div class="package-icon" style="background: linear-gradient(135deg, #F77737, #E1306C);">❤️</div>
      <div class="package-amount">2500</div>
      <div class="package-type">Like-uri</div>
      <div class="package-price">49.99 <span class="package-currency">RON</span></div>
      <ul class="package-features">
        <li><span class="check">✓</span> Livrare Rapidă</li>
        <li><span class="check">✓</span> Pe orice postare</li>
        <li><span class="check">✓</span> Fără parolă</li>
        <li><span class="check">✓</span> Garanție completă</li>
      </ul>
      {% if product %}
        {% for variant in product.variants %}
          {% if variant.title contains '2500 Like' %}
            <a href="/cart/{{ variant.id }}:1" class="package-btn">⚡ Cumpără Acum</a>
          {% endif %}
        {% endfor %}
      {% else %}
        <a href="/products/{{ section.settings.product_handle }}" class="package-btn">⚡ Cumpără Acum</a>
      {% endif %}
      <div class="package-delivery">⚡ Livrare Rapidă</div>
    </div>

    <!-- 5000 Like-uri -->
    <div class="package-card">
      <div class="package-icon" style="background: linear-gradient(135deg, #F77737, #E1306C);">❤️</div>
      <div class="package-amount">5000</div>
      <div class="package-type">Like-uri</div>
      <div class="package-price">69.99 <span class="package-currency">RON</span></div>
      <ul class="package-features">
        <li><span class="check">✓</span> Livrare Rapidă</li>
        <li><span class="check">✓</span> Pe orice postare</li>
        <li><span class="check">✓</span> Fără parolă</li>
        <li><span class="check">✓</span> Garanție completă</li>
      </ul>
      {% if product %}
        {% for variant in product.variants %}
          {% if variant.title contains '5000 Like' %}
            <a href="/cart/{{ variant.id }}:1" class="package-btn">⚡ Cumpără Acum</a>
          {% endif %}
        {% endfor %}
      {% else %}
        <a href="/products/{{ section.settings.product_handle }}" class="package-btn">⚡ Cumpără Acum</a>
      {% endif %}
      <div class="package-delivery">⚡ Livrare Rapidă</div>
    </div>
  </div>

  <!-- Extra Offer -->
  <div class="extra-offer fade-up">
    <h3>🎁 Adaugă Extra la Super Preț!</h3>
    <p>+500 Extra Urmăritori sau Like-uri</p>
    <div class="extra-price">9.99 RON</div>
    {% if product %}
      {% for variant in product.variants %}
        {% if variant.title contains 'Extra' or variant.title contains '+500' %}
          <a href="/cart/{{ variant.id }}:1" class="extra-btn">CUMPĂRĂ ACUM</a>
        {% endif %}
      {% endfor %}
    {% else %}
      <a href="/products/{{ section.settings.product_handle }}" class="extra-btn">CUMPĂRĂ ACUM</a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Pachete Like-uri",
  "settings": [
    {
      "type": "text",
      "id": "product_handle",
      "label": "Product handle (URL slug)",
      "default": "servicii-media-instagram",
      "info": "Handle-ul produsului din Shopify"
    }
  ],
  "presets": [
    {
      "name": "Pachete Like-uri"
    }
  ]
}
{% endschema %}
